# 班级朗读记录系统

一个轻量的 PHP + MySQL 多班级晨读 / 晚读打卡系统，供班干部在早读、晚读时段快速记录全班朗读情况，并按日 / 周 / 月 / 学期 / 总统计排行。

## 功能

- **多班级共用一套站点**：登录 = 班级 + 密码；内置 14 个班，初始密码 `admin + 两位班级号`（一班 = `admin01`，十四班 = `admin14`）
- **教师管理界面**（`admin.php`，独立管理密码）：
  - 修改任意班级的登录密码
  - 维护各班学生名单（添加 / 改名 / 删除，姓名自动 base64 转码存储，兼容不支持中文的数据库）
  - 清空某班的全部记录数据
- 早读 / 晚读两种记录类型，仅在指定时间段内开放操作
- 学生卡片一键「+ 加分 / − 扣分」，点击即时反馈（波纹动画 + 底部提示），不等服务器回传；网络异常有明确提示
- **一次朗读时间最多加一分**（本次朗读已加过则锁定并弹出提示），**扣分不限**；加分优先抵扣已有扣分
- 已记录卡片盖绿色「优秀」印章，有扣分待补齐的卡片盖红色「差」印章（整周显示）；本次朗读已加过分的卡片带「已加分」角标
- 统计页支持**今日 / 本周 / 本月 / 学期 / 总统计**：本周显示净分（加分−扣分），今日 / 本月 / 学期 / 总统计按正分次数计、不计负分；学期按配置的开学日期分割
- 图表使用本地 Chart.js（无需访问 CDN，校园网友好）
- 所有弹窗均为页面自绘大字弹窗（不用浏览器原生 alert/confirm），方便老师从远处看到

## 界面

「纸墨 · 印章」主题：米白纸面、墨黑通栏、朱红印章点缀；已记录卡片盖绿色「优秀」印章，有扣分的卡片盖红色「差」印章，移动端自适应。

## 部署

1. 环境要求：PHP 8.x + MySQL（PDO 扩展），需先在数据库服务商处建好数据库。
2. 复制配置模板并填写真实信息：

   ```bash
   cp config.example.php config.php
   ```

   在 `config.php` 中填写数据库连接、教师管理密码 `ADMIN_PASSWORD`、班级数量 `CLASS_COUNT` 与学期开始日期。
3. 将全部文件上传到站点目录，访问 `index.php` 即可；首次访问会自动建表并创建各班。

## 注意

- `config.php` 含真实数据库凭据，已被 `.gitignore` 排除，**禁止提交到公开仓库**；公开仓库只保留 `config.example.php` 模板。
- 学生姓名以 base64 存储于 `name_encoded` 列，读取时自动解码显示，数据库字符集无中文也不影响。
